---
phase: 1
slug: infrastructure-and-skeleton
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-01
---

# Phase 1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | rspec-rails 8.0.4 |
| **Config file** | `.rspec` + `spec/spec_helper.rb` + `spec/rails_helper.rb` (created by `rails generate rspec:install`) |
| **Quick run command** | `docker compose run --rm web bundle exec rspec` |
| **Full suite command** | `docker compose run --rm web bundle exec rspec --format documentation` |
| **Estimated runtime** | ~30 seconds |

---

## Sampling Rate

- **After every task commit:** Run `docker compose run --rm web bundle exec rspec`
- **After every plan wave:** Run `docker compose run --rm web bundle exec rspec --format documentation`
- **Before `/gsd:verify-work`:** Full suite must be green + `curl http://localhost:3000` returns 200
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 1-01-01 | 01 | 0 | QUAL-01 | unit | `docker compose run --rm web bundle exec rspec` | ❌ W0 | ⬜ pending |
| 1-01-02 | 01 | 1 | INFRA-01 | smoke | `docker compose up -d && curl -f http://localhost:3000` | ❌ W0 | ⬜ pending |
| 1-01-03 | 01 | 1 | INFRA-02 | structural | `grep -n 'V2' docker-compose.yml` | ❌ W0 | ⬜ pending |
| 1-01-04 | 01 | 1 | INFRA-03 | unit | `docker compose run --rm web bundle exec rspec spec/smoke_spec.rb` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `spec/smoke_spec.rb` — minimal "RSpec wiring works" spec; also verifies no DB queries on request (INFRA-03)
- [ ] `spec/spec_helper.rb` — SimpleCov config must be first lines
- [ ] `spec/rails_helper.rb` — VCR + WebMock configuration
- [ ] `.rspec` — generated by `rails generate rspec:install`
- [ ] Framework install: `bundle exec rails generate rspec:install` — once Gemfile has rspec-rails

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| `docker compose up` starts app at localhost:3000 with no manual steps | INFRA-01 | Requires Docker daemon and port binding, can't run in CI container-in-container | Run `docker compose up -d`; wait 10s; run `curl -f http://localhost:3000`; expect HTTP 200 |
| Docker Compose has commented `db:` service and `DATABASE_URL` env var for V2 | INFRA-02 | Structural review of YAML comments | Open `docker-compose.yml`; confirm `# db:` service block exists and `DATABASE_URL` env var is present with comment referencing V2 |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
